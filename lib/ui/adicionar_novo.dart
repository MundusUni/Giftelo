import 'package:flutter/material.dart';

class AdicionarNovo extends StatelessWidget {
  const AdicionarNovo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Adicionar Novo")),
    );
  }
}