import 'package:flutter/material.dart';

class ListaDeCadastrados extends StatelessWidget {
  const ListaDeCadastrados({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Lista de Cadastrados")),
    );
  }
}