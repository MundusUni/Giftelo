import 'package:flutter/material.dart';

class LayoutDosCartoes extends StatelessWidget {
  const LayoutDosCartoes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Adicionar Novo")),
    );
  }
}